[package]
name = "native"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
libc = "0.2.146"
once_cell = "1.18.0"
quake3-native-vm = { path = "../../quake3-native-vm", version = "0.1.0" }
